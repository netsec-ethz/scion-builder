diff --git a/env/common.sh b/env/common.sh
old mode 100644
new mode 100755
index f66d2b5c..8dc7b467
--- a/env/common.sh
+++ b/env/common.sh
@@ -1,8 +1,3 @@
-if [ $(id -u) = "0" ]; then
-    echo "ERROR: Running $0 as root is not supported (and is a bad idea, anyway)"
-    exit 1
-fi
-
 lower() {
     tr '[:upper:]' '[:lower:']
 }
diff --git a/env/debian/deps b/env/debian/deps
index 015bf803..15cae479 100755
--- a/env/debian/deps
+++ b/env/debian/deps
@@ -10,4 +10,4 @@ APTARGS=${APTARGS:---no-remove}
 pkgs="$($BASE/check)"
 [ -z "$pkgs" ] && exit
 
-sudo DEBIAN_FRONTEND=noninteractive apt-get install $APTARGS --no-install-recommends $pkgs
+DEBIAN_FRONTEND=noninteractive apt-get -y install $APTARGS --no-install-recommends $pkgs
